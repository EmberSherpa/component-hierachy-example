{{#async-tree
  fetch=fetch
  checkOpen=checkOpen
  open="open"
  as |node isOpen hasChildren|
}}
  {{#if node}}
    {{#if hasChildren}}
      {{#if isOpen}}
        <button {{action 'close' node}}>-</button>
      {{else}}
        <button {{action 'open' node}}>+</button>
      {{/if}}
    {{/if}}
    {{#component (get-component-name node.type)}}
    {{node.name}}
    {{/component}}
  {{/if}}
{{/async-tree}}
