{{#async-tree
  fetch=fetch
  checkOpen=checkOpen
  open="open"
  close="close"
  as |child parent|
}}
  {{#async-tree
    node=child
    parent=parent
    fetch=fetch
    checkOpen=checkOpen
    open="open"
    close="close"
    as |child parent|
    }}
    {{#async-tree
      node=child
      parent=parent
      fetch=fetch
      checkOpen=checkOpen
      open="open"
      close="close"
      as |child parent|
    }}
      {{async-tree
        node=child
        parent=parent
        fetch=fetch
        checkOpen=checkOpen
        open="open"
        close="close"
      }}
    {{/async-tree}}
  {{/async-tree}}
{{/async-tree}}
